# Reward formulation:
# R_t = (completion_weight * completed_jobs)
#       - (waiting_weight * total_queued_wait_penalty)
#       - (idle_gpu_weight * idle_gpus_fraction)
#       - (deadline_miss_weight * deadline_violations)
#       - (invalid_action_penalty)

completion_weight: 2.0
waiting_weight: 0.005
idle_gpu_weight: 0.02
deadline_miss_weight: 3.0
invalid_action_penalty: 1.5
starvation_penalty_weight: 0.05
step_penalty: 0.001
